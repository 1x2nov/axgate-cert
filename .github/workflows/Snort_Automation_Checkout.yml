name : Checkout 
on : 
  push :
    branches : 
      - master
    paths :
      - "rule_test/test"

jobs :
  build :
    runs-on : self-hosted
    steps :
    - name : Checkout Snort Automation
      uses: actions/checkout@v4
      with:
        sparse-checkout: |
            snort_automation.py
        sparse-checkout-cone-mode: false
