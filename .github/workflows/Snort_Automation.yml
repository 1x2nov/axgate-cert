name : Snort Automation
on :
  workflow_run :
    workflows : [Checkout Snort Automation]
    types : 
      - completed
      
jobs : 
  build :
    runs-on : self-hosted
    steps :

    - name : execute
      run : python snort_automation.py
