name : Github Actions Test
on :
  push :
    branches : master


jobs :
  build :
    runs-on : ubuntu-latest
    steps :
      - name : Snort Docker Test
        run : |
          docker run --rm -it dnif/snort /bin/bash
          ls -al
